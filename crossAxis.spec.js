import { scale, domain } from '../perspective/packages/perspective-viewer-d3fc/src/js/axis/crossAxis';

describe('First Test', () => {
    const c = null;

    test('should be null if no data has been specified', () => {
        expect(c).toBeNull();
    });

    console.log("Here");
});